export class user { 
    constructor(
        id,
        id_order,
        id_product,
        quantity,
        total_price,
    ){ 
        this.id = id,
        this.id_order = id_order
        this.id_product = id_product
        this.quantity = quantity
        this.total_price = total_price
    }
}